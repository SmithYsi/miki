/** Easing y variantes compartidas para Framer Motion (un solo lenguaje de animación). */
export const EASE = [0.16, 1, 0.3, 1] as const

export const viewportOnce = { once: true, amount: 0.25 } as const
